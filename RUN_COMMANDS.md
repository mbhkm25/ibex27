# أوامر تشغيل مشروع IBEX27

## 📋 الخطوات السريعة

### 1️⃣ تثبيت المتطلبات (مرة واحدة فقط)
```bash
cd ibex27
npm install
```

### 2️⃣ تطبيق Migrations على قاعدة البيانات
```bash
npm run db:push
```

### 3️⃣ تشغيل التطبيق
```bash
npm run dev
```

---

## 🔧 أوامر مفصلة

### قاعدة البيانات

#### تطبيق Migrations مباشرة
```bash
npm run db:push
```
**متى تستخدمه:** عند أول تشغيل أو بعد تعديل schema.ts

#### توليد Migrations جديدة
```bash
npm run db:generate
```
**متى تستخدمه:** بعد تعديل schema.ts لإنشاء ملفات SQL migrations

#### فتح Drizzle Studio (واجهة قاعدة البيانات)
```bash
npm run db:studio
```
**ماذا يفعل:** يفتح متصفح مع واجهة لتصفح قاعدة البيانات

---

### التطوير

#### تشغيل في وضع التطوير
```bash
npm run dev
```
**الميزات:**
- Hot Module Replacement (HMR)
- DevTools متاح (F12)
- تحديث تلقائي عند تغيير الكود

#### التحقق من أنواع TypeScript
```bash
npm run typecheck
```
**يفحص:** جميع ملفات TypeScript للأخطاء

---

### البناء

#### بناء التطبيق للإنتاج
```bash
npm run build
```
**ينشئ:** ملفات جاهزة في مجلد `out/`

#### تشغيل النسخة المبنية
```bash
npm start
```
**يشغل:** النسخة المبنية من `out/`

---

## 🔐 معلومات تسجيل الدخول

### مدير المنصة (Platform Admin)
- **البريد:** `admin@ibex.com`
- **كلمة المرور:** `admin123`
- **الدور:** `platform_admin`

> **ملاحظة:** سيتم إنشاء هذا المستخدم تلقائياً عند أول تشغيل

---

## 📝 سيناريو التشغيل الكامل

```bash
# 1. الانتقال إلى مجلد المشروع
cd ibex27

# 2. تثبيت المتطلبات (إذا لم تكن مثبتة)
npm install

# 3. تطبيق migrations على قاعدة البيانات
npm run db:push

# 4. تشغيل التطبيق
npm run dev
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: فشل الاتصال بقاعدة البيانات
```bash
# تحقق من connection string في:
# src/main/db.ts
```

### المشكلة: Migrations فشلت
```bash
# جرب تطبيق migrations يدوياً:
npm run db:push
```

### المشكلة: أخطاء TypeScript
```bash
# تحقق من الأخطاء:
npm run typecheck
```

---

## 📚 أوامر إضافية

### تنسيق الكود
```bash
npm run format
```

### فحص الكود
```bash
npm run lint
```

### بناء لنظام Windows
```bash
npm run build:win
```

### بناء لنظام macOS
```bash
npm run build:mac
```

### بناء لنظام Linux
```bash
npm run build:linux
```

---

## ✅ بعد التشغيل

1. سجل دخول كمدير المنصة
2. أنشئ تاجر جديد
3. أنشئ متجر للتاجر
4. جرب تسجيل عميل جديد
5. جرب عملية بيع

---

## 💡 نصائح

- استخدم **F12** لفتح DevTools
- راقب **Console** للأخطاء
- التغييرات في React تظهر **فوراً**
- استخدم `npm run typecheck` قبل البناء

