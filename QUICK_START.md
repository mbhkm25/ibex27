# 🚀 دليل البدء السريع - IBEX27

## ⚡ التشغيل السريع (3 خطوات)

```bash
# 1. تثبيت المتطلبات
cd ibex27
npm install

# 2. تطبيق Migrations
npm run db:push

# 3. تشغيل التطبيق
npm run dev
```

---

## 📋 أوامر التشغيل الرئيسية

| الأمر | الوصف |
|------|-------|
| `npm run dev` | تشغيل في وضع التطوير |
| `npm run build` | بناء التطبيق للإنتاج |
| `npm start` | تشغيل النسخة المبنية |
| `npm run db:push` | تطبيق migrations على قاعدة البيانات |
| `npm run db:generate` | توليد migrations جديدة |
| `npm run db:studio` | فتح واجهة قاعدة البيانات |
| `npm run typecheck` | التحقق من أنواع TypeScript |

---

## 🔐 تسجيل الدخول الافتراضي

**مدير المنصة:**
- البريد: `admin@ibex.com`
- كلمة المرور: `admin123`

---

## 📁 هيكل المشروع

```
ibex27/
├── src/
│   ├── main/          # Backend (Electron Main)
│   ├── preload/       # IPC Bridge
│   ├── renderer/      # Frontend (React)
│   └── shared/        # Shared Code
├── drizzle/           # Database Migrations
└── package.json
```

---

## ✅ الخطوات بعد التشغيل

1. ✅ سجل دخول كمدير المنصة
2. ✅ أنشئ تاجر جديد من لوحة التحكم
3. ✅ أنشئ متجر للتاجر
4. ✅ جرب تسجيل عميل جديد
5. ✅ جرب عملية بيع مع عميل

---

## 🆘 مساعدة

- راجع `START_GUIDE.md` للدليل الكامل
- راجع `RUN_COMMANDS.md` لجميع الأوامر
- اضغط **F12** لفتح DevTools

