# معلومات المستخدمين التجريبيين - Test Users Information

## 📍 موقع البيانات

**مهم:** بيانات المستخدمين موجودة في **قاعدة البيانات السحابية (PostgreSQL/Neon)** وليس في قاعدة البيانات المحلية (SQLite).

- **قاعدة البيانات السحابية:** PostgreSQL على Neon
- **قاعدة البيانات المحلية:** SQLite في `data/cashier.db` (للمبيعات والمنتجات فقط)

## 👥 بيانات المستخدمين التجريبيين

### 1. 👑 أدمن المنصة (Platform Admin)
- **البريد:** `admin@ibex.com`
- **كلمة المرور:** `admin123`
- **الدور:** `platform_admin`
- **الواجهة:** `/admin`

### 2. 🏪 التاجر (Merchant)
- **البريد:** `merchant@example.com`
- **كلمة المرور:** `merchant123`
- **الدور:** `merchant`
- **الواجهة:** `/`

### 3. 💰 الكاشير (Cashier)
- **البريد:** `cashier@example.com`
- **كلمة المرور:** `cashier123`
- **الدور:** `cashier`
- **الواجهة:** `/`

### 4. 👤 العميل (Customer)
- **رقم الجوال:** `771234567`
- **كلمة المرور:** `customer123`
- **الدور:** `customer`
- **الواجهة:** `/customer/dashboard`

## 🔧 الأوامر المتاحة

### التحقق من وجود المستخدمين:
```bash
npm run db:check-users
```

### إنشاء/تحديث المستخدمين:
```bash
npm run db:seed-users
```

## 📝 ملاحظات

1. **البيانات في السحابة:** جميع بيانات المستخدمين (users, customers) موجودة في قاعدة البيانات السحابية
2. **البيانات المحلية:** قاعدة البيانات المحلية (SQLite) تحتوي فقط على:
   - المنتجات (products)
   - المبيعات (sales)
   - العملاء المحليين (customers) - نسخة محلية للمزامنة
   - قائمة المزامنة (sync_queue)

3. **المزامنة:** البيانات المحلية يتم مزامنتها مع السحابة تلقائياً عند توفر الاتصال

## 🗄️ الجداول في قاعدة البيانات السحابية

- `users` - المستخدمون (أدمن، تاجر، كاشير)
- `customers` - العملاء
- `stores` - المتاجر
- `customer_store_relations` - علاقات العملاء بالمتاجر

## 🔍 كيفية التحقق من البيانات

### طريقة 1: استخدام Drizzle Studio
```bash
npm run db:studio
```

### طريقة 2: استخدام السكريبت
```bash
npm run db:check-users
```

### طريقة 3: استخدام SQL مباشرة
اتصل بقاعدة البيانات السحابية واستخدم:
```sql
SELECT id, name, email, role, status FROM users WHERE deleted_at IS NULL;
SELECT id, name, phone, registration_status FROM customers WHERE deleted_at IS NULL;
```

