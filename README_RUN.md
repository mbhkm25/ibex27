# 🎯 أوامر تشغيل مشروع IBEX27

## ⚡ البدء السريع

```bash
# 1. الانتقال إلى مجلد المشروع
cd ibex27

# 2. تثبيت المتطلبات (إذا لم تكن مثبتة)
npm install

# 3. تطبيق Migrations على قاعدة البيانات
npm run db:push

# 4. تشغيل التطبيق في وضع التطوير
npm run dev
```

---

## 📝 الأوامر الأساسية

### تطوير
```bash
npm run dev          # تشغيل في وضع التطوير (Hot Reload)
npm run build        # بناء التطبيق للإنتاج
npm start            # تشغيل النسخة المبنية
```

### قاعدة البيانات
```bash
npm run db:push      # تطبيق migrations مباشرة على قاعدة البيانات
npm run db:generate  # توليد migrations جديدة من schema
npm run db:studio    # فتح واجهة Drizzle Studio
```

### التحقق
```bash
npm run typecheck    # التحقق من أنواع TypeScript
npm run lint         # فحص الكود
npm run format       # تنسيق الكود
```

---

## 🔐 معلومات تسجيل الدخول

**مدير المنصة (Platform Admin):**
- البريد الإلكتروني: `admin@ibex.com`
- كلمة المرور: `admin123`
- الدور: `platform_admin`

> سيتم إنشاء هذا المستخدم تلقائياً عند أول تشغيل

---

## 📋 سيناريو التشغيل الكامل

### الخطوة 1: الإعداد الأولي
```bash
cd ibex27
npm install
```

### الخطوة 2: تطبيق Migrations
```bash
npm run db:push
```
هذا الأمر سيطبق جميع جداول قاعدة البيانات.

### الخطوة 3: تشغيل التطبيق
```bash
npm run dev
```
سيتم فتح نافذة Electron مع التطبيق.

---

## 🎮 بعد التشغيل

1. **سجل دخول كمدير المنصة:**
   - استخدم `admin@ibex.com` / `admin123`

2. **أنشئ تاجر جديد:**
   - من لوحة التحكم → إدارة التجار → إضافة تاجر

3. **أنشئ متجر:**
   - من لوحة التحكم → إدارة المتاجر → إضافة متجر

4. **جرب تسجيل عميل:**
   - من رابط المتجر (Customer Portal)

5. **جرب عملية بيع:**
   - من صفحة نقاط البيع

---

## 🛠️ أوامر إضافية

### بناء للتوزيع
```bash
npm run build:win     # Windows
npm run build:mac     # macOS
npm run build:linux   # Linux
```

### استكشاف الأخطاء
```bash
# فتح DevTools في التطبيق: اضغط F12
# مراقبة Console للأخطاء
```

---

## 📚 ملفات التوثيق

- `QUICK_START.md` - دليل البدء السريع
- `START_GUIDE.md` - دليل شامل
- `RUN_COMMANDS.md` - جميع الأوامر
- `ARCHITECTURE.md` - بنية المشروع

---

## ⚠️ ملاحظات مهمة

1. **قاعدة البيانات:** التطبيق متصل بقاعدة بيانات Neon (PostgreSQL)
2. **Migrations:** يتم تطبيقها تلقائياً عند التشغيل، أو يدوياً بـ `db:push`
3. **Hot Reload:** التغييرات في React تظهر فوراً في وضع التطوير
4. **DevTools:** اضغط F12 لفتح أدوات المطور

---

## 🆘 حل المشاكل

### المشكلة: فشل الاتصال بقاعدة البيانات
- تحقق من اتصال الإنترنت
- تحقق من connection string في `src/main/db.ts`

### المشكلة: Migrations فشلت
- جرب `npm run db:push` يدوياً
- تحقق من وجود مجلد `drizzle/`

### المشكلة: التطبيق لا يبدأ
- احذف `node_modules` و `package-lock.json`
- شغل `npm install` مرة أخرى
- شغل `npm run build` ثم `npm start`

---

## ✅ جاهز للبدء!

الآن يمكنك تشغيل التطبيق باستخدام:
```bash
npm run dev
```

بالتوفيق! 🚀

