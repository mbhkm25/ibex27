# 🧪 اختبار نسخة الويب محلياً

قبل رفع المشروع على Vercel، يمكنك اختبار نسخة الويب محلياً.

## 📋 المتطلبات

1. Node.js 18+
2. قاعدة بيانات Neon (موجودة بالفعل)

## 🚀 خطوات الاختبار

### 1. إعداد Environment Variables محلياً

أنشئ ملف `.env.local` في جذر المشروع:

```env
DATABASE_URL=postgresql://neondb_owner:npg_81FTgGbhISvQ@ep-morning-union-a7vq0d6n-pooler.ap-southeast-2.aws.neon.tech/neondb?sslmode=require
NEXT_PUBLIC_WEB_URL=http://localhost:5174
```

### 2. بناء نسخة الويب

```bash
npm run build:web
```

سيتم إنشاء مجلد `dist-web` يحتوي على الملفات المبنية.

### 3. تشغيل محلي (طريقتان)

#### الطريقة A: استخدام Vite Dev Server (موصى بها للتطوير)

```bash
npx vite --config vite.web.config.ts --port 5174
```

ثم افتح: `http://localhost:5174`

#### الطريقة B: استخدام serve (لاختبار البناء النهائي)

```bash
npx serve dist-web -p 5174
```

ثم افتح: `http://localhost:5174`

### 4. اختبار API Functions محلياً

**ملاحظة:** Vercel Serverless Functions لا تعمل محلياً مباشرة. ستحتاج إلى:

#### خيار 1: استخدام Vercel CLI (موصى به)

```bash
# تثبيت Vercel CLI
npm i -g vercel

# تشغيل محلي مع Functions
vercel dev
```

سيشغل Vercel محلياً مع دعم Serverless Functions.

#### خيار 2: Mock API (للاختبار السريع)

يمكنك تعديل `web-adapter.ts` مؤقتاً لاستخدام بيانات تجريبية.

### 5. اختبار QR Code

1. افتح تطبيق Electron (في نافذة منفصلة)
2. سجل دخول كتاجر
3. اذهب إلى "إعدادات المتجر"
4. اضغط "عرض كود QR للمتجر"
5. افتح `http://localhost:5174` في المتصفح
6. امسح QR Code من تطبيق Electron
7. يجب أن يفتح الرابط في المتصفح!

## 🔍 اختبار الصفحات

### صفحة الهبوط:
```
http://localhost:5174/store/test-store
```

### تسجيل الدخول:
```
http://localhost:5174/customer/login
```

### لوحة التحكم:
```
http://localhost:5174/customer/dashboard
```

## ⚠️ ملاحظات

1. **API Functions:**
   - في Vercel، Functions تعمل تلقائياً
   - محلياً، استخدم `vercel dev` لدعم Functions

2. **CORS:**
   - إذا واجهت مشاكل CORS، تأكد من أن API Functions ترجع Headers صحيحة

3. **Database:**
   - تأكد من أن `DATABASE_URL` صحيح
   - تأكد من أن قاعدة البيانات متاحة من الإنترنت

## 🐛 استكشاف الأخطاء

### مشكلة: "Failed to fetch" عند استدعاء API

**الحل:**
- استخدم `vercel dev` بدلاً من `vite`
- أو تأكد من أن API Functions تعمل بشكل صحيح

### مشكلة: QR Code لا يفتح الصفحة

**الحل:**
- تأكد من `NEXT_PUBLIC_WEB_URL` مضبوط على `http://localhost:5174`
- تأكد من أن المتجر له `slug` صحيح

### مشكلة: الصفحة بيضاء

**الحل:**
- افتح Console في المتصفح (F12)
- تحقق من الأخطاء
- تأكد من أن `web-adapter.ts` يتم استدعاؤه

---

**بعد التأكد من أن كل شيء يعمل محلياً، يمكنك المتابعة إلى رفع المشروع على Vercel! 🚀**

