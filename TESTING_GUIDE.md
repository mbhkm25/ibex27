# دليل الاختبار - Testing Guide

## 🧪 اختبار المزامنة التلقائية

### 1. التشغيل الأول (Smoke Test)

```bash
npm run dev
```

**ما يجب التحقق منه:**

1. ✅ **Console لا يحتوي على أخطاء**
   - افتح DevTools (F12)
   - تحقق من Console
   - يجب أن ترى: `✅ Local SQLite database initialized`

2. ✅ **ملف قاعدة البيانات المحلية**
   - ابحث عن مجلد `data/` في جذر المشروع
   - يجب أن تجد ملف `cashier.db`
   - إذا لم تجده، تحقق من Console للأخطاء

3. ✅ **مكون حالة المزامنة**
   - بعد تسجيل الدخول واختيار متجر
   - يجب أن ترى أيقونة سحابة في Header
   - الألوان:
     - 🟢 **أخضر**: مزامن بنجاح
     - 🟡 **أصفر**: مبيعات معلقة أو خطأ
     - 🔴 **أحمر**: غير متصل بالإنترنت

### 2. اختبار المزامنة التلقائية

#### عند بدء التطبيق:
1. سجل الدخول كـ merchant أو cashier
2. اختر متجر
3. **يجب أن ترى في Console:**
   ```
   🔄 Starting auto sync for store: [ID]
   🔄 Performing initial sync...
   ✅ Initial sync completed: X pulled, Y pushed
   ✅ Auto sync started (every 5 minutes)
   ```

#### المزامنة الدورية:
- كل 5 دقائق، يجب أن ترى في Console:
  ```
  🔄 Performing quick sync...
  ✅ Quick sync completed: X sales pushed
  ```

### 3. اختبار المزامنة اليدوية

من Console في DevTools:
```javascript
// الحصول على حالة المزامنة
await window.api.sync.status(1) // استبدل 1 بـ storeId

// مزامنة كاملة
await window.api.sync.full(1)

// مزامنة سريعة
await window.api.sync.quick(1)
```

### 4. اختبار بدون إنترنت

1. **افصل الإنترنت**
2. **سجل مبيعات جديدة**
3. **يجب أن ترى:**
   - أيقونة السحابة 🔴 حمراء
   - نص: "غير متصل"
   - المبيعات تُسجل محلياً في SQLite

4. **أعد الاتصال بالإنترنت**
5. **انتظر 5 دقائق أو شغل مزامنة يدوية**
6. **يجب أن ترى:**
   - المبيعات تُرفع تلقائياً
   - أيقونة السحابة 🟢 خضراء

### 5. التحقق من البيانات

#### في SQLite المحلي:
```bash
# استخدم أداة SQLite
sqlite3 data/cashier.db

# عرض المبيعات المعلقة
SELECT * FROM sales WHERE sync_status = 'pending';

# عرض المنتجات المحلية
SELECT * FROM products LIMIT 10;
```

#### في Neon (السحابي):
- استخدم Drizzle Studio:
  ```bash
  npm run db:studio
  ```

## 🐛 استكشاف الأخطاء

### المشكلة: قاعدة بيانات SQLite لا تُنشأ

**الحل:**
1. تحقق من صلاحيات الكتابة في مجلد المشروع
2. تحقق من Console للأخطاء
3. تأكد من تثبيت `better-sqlite3`:
   ```bash
   npm install better-sqlite3
   ```

### المشكلة: المزامنة لا تعمل

**الحل:**
1. تحقق من اتصال الإنترنت
2. تحقق من `DATABASE_URL` في `.env`
3. راجع Console للأخطاء
4. جرب مزامنة يدوية:
   ```javascript
   await window.api.sync.full(storeId)
   ```

### المشكلة: مكون SyncStatus لا يظهر

**الحل:**
1. تأكد من تسجيل الدخول
2. تأكد من اختيار متجر
3. تحقق من Console للأخطاء
4. تأكد من أن `storeId` موجود في `selectedStore`

## ✅ قائمة التحقق النهائية

- [ ] التطبيق يعمل بدون أخطاء
- [ ] ملف `data/cashier.db` موجود
- [ ] مكون SyncStatus يظهر في Header
- [ ] المزامنة التلقائية تعمل عند بدء التطبيق
- [ ] المزامنة الدورية تعمل كل 5 دقائق
- [ ] المزامنة تعمل بدون إنترنت (تسجيل محلي)
- [ ] المزامنة تعمل عند عودة الاتصال
- [ ] الأيقونة تتغير حسب الحالة (أخضر/أصفر/أحمر)

## 📝 ملاحظات

- المزامنة التلقائية تبدأ فقط عند اختيار متجر
- المزامنة الدورية تعمل في الخلفية بشكل صامت
- يمكن إيقاف المزامنة التلقائية من Console:
  ```javascript
  await window.api.sync.stopAuto()
  ```

