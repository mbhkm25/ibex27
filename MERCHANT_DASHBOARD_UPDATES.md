# تحديثات لوحة تحكم التاجر

## ما تم إنجازه

### 1. تحديث صفحة العملاء (Customers.tsx)
**التحسينات:**
- ✅ إضافة عمود "حالة التسجيل" في الجدول
- ✅ عرض حالة التسجيل (pending, approved, rejected)
- ✅ أزرار موافقة/رفض مباشرة في الجدول للطلبات المعلقة
- ✅ تمييز العملاء التقليديين (بدون registrationStatus)

**الميزات:**
- عرض حالة التسجيل بشكل واضح مع ألوان:
  - 🟡 قيد المراجعة (pending)
  - 🟢 موافق عليه (approved)
  - 🔴 مرفوض (rejected)
- أزرار سريعة للموافقة/الرفض مباشرة من الجدول
- تحديث تلقائي بعد الموافقة/الرفض

---

### 2. صفحة طلبات التسجيل (CustomerRegistrationRequests.tsx)
**المسار:** `/customer-registration-requests`

**الوظيفة:**
- عرض جميع طلبات تسجيل العملاء
- نظام فلترة:
  - قيد المراجعة (pending)
  - موافق عليها (approved)
  - مرفوضة (rejected)
  - الكل (all)
- عداد للطلبات المعلقة في الأعلى

**الميزات:**
- عرض تفاصيل العميل:
  - الاسم
  - رقم الجوال
  - رقم واتساب (إن وجد)
  - تاريخ التسجيل
  - الملاحظات (إن وجدت)
- أزرار موافقة/رفض واضحة
- تصميم بطاقات سهل القراءة
- تحديث تلقائي بعد الإجراءات

---

### 3. صفحة طلبات تعبئة الرصيد (BalanceRequests.tsx)
**المسار:** `/balance-requests`

**الوظيفة:**
- عرض جميع طلبات تعبئة الرصيد
- اختيار المتجر (للتجار متعددي المتاجر)
- نظام فلترة:
  - قيد المراجعة (pending)
  - موافق عليها (approved)
  - مرفوضة (rejected)
  - الكل (all)

**الميزات:**
- عرض تفاصيل الطلب:
  - رقم الطلب
  - البنك أو المصرف
  - المبلغ
  - الرقم المرجعي
  - تاريخ الطلب
  - معلومات العميل
- أزرار موافقة/رفض
- عند الموافقة:
  - يتم إضافة المبلغ تلقائياً إلى رصيد العميل في المتجر
  - يتم إنشاء سجل في customer_transactions
  - يتم تحديث حالة الطلب
- عرض تاريخ الموافقة للطلبات المعتمدة

---

### 4. تحديثات القائمة الجانبية (Sidebar.tsx)
**الإضافات:**
- ✅ رابط "طلبات التسجيل" (UserPlus icon)
- ✅ رابط "طلبات الرصيد" (Wallet icon)

**الترتيب:**
- تم إضافة الروابط بعد "العملاء" مباشرة
- أيقونات واضحة ومميزة

---

### 5. تحديثات Routing (App.tsx)
**المسارات الجديدة:**
```typescript
<Route path="customer-registration-requests" element={<CustomerRegistrationRequests />} />
<Route path="balance-requests" element={<BalanceRequests />} />
```

---

### 6. تحديثات Backend (customers.ts)
**التحسينات:**
- ✅ إضافة فلترة `deletedAt` (Soft Delete)
- ✅ دعم البحث مع Soft Delete

---

## API Calls المستخدمة

### Customer Auth:
- `window.api.customerAuth.approve(customerId)` - موافقة على التسجيل
- `window.api.customerAuth.reject(customerId)` - رفض التسجيل

### Customer Balance:
- `window.api.customerBalance.getRequests(storeId)` - جلب طلبات الرصيد
- `window.api.customerBalance.approve({ requestId, approvedBy })` - موافقة على طلب رصيد
- `window.api.customerBalance.reject(requestId)` - رفض طلب رصيد

### Stores:
- `window.api.stores.getMerchantStores(merchantId)` - جلب متاجر التاجر

---

## التدفق الكامل

### تسجيل عميل جديد:
1. العميل يفتح رابط التسجيل: `/customer/store/:slug/register`
2. يملأ البيانات ويرسل الطلب
3. الطلب يظهر في:
   - صفحة "طلبات التسجيل" (حالة: pending)
   - صفحة "العملاء" (عمود حالة التسجيل)
4. التاجر يقرر:
   - ✅ موافقة → العميل يمكنه تسجيل الدخول
   - ❌ رفض → الطلب مرفوض

### طلب تعبئة رصيد:
1. العميل يطلب تعبئة رصيد من واجهته
2. الطلب يظهر في صفحة "طلبات الرصيد" (حالة: pending)
3. التاجر يتحقق من:
   - البنك
   - المبلغ
   - الرقم المرجعي
4. التاجر يقرر:
   - ✅ موافقة → يتم إضافة المبلغ تلقائياً + سجل عملية
   - ❌ رفض → الطلب مرفوض

---

## التصميم

- **الألوان:**
  - 🟡 أصفر للطلبات المعلقة
  - 🟢 أخضر للموافقة
  - 🔴 أحمر للرفض
- **الأيقونات:** Lucide React
- **التخطيط:** بطاقات واضحة مع معلومات منظمة
- **التفاعل:** أزرار واضحة مع تأكيد قبل الإجراءات

---

## الخطوات التالية

1. ✅ تحديث صفحة العملاء - **تم**
2. ✅ صفحة طلبات التسجيل - **تم**
3. ✅ صفحة طلبات الرصيد - **تم**
4. ⏳ إضافة إشعارات للطلبات الجديدة
5. ⏳ إضافة تصدير للتقارير
6. ⏳ إضافة بحث متقدم


